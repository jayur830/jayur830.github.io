'use client';

import { Grid, Input } from '@mui/material';

export default function Admin() {
  return (
    <Grid
      container
      flexDirection="column"
    >
      <Grid>
        <Input />
      </Grid>
    </Grid>
  );
}
